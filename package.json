{
  "name": "relay-notes-api",
  "version": "0.1.0",
  "description": "Relay Notes API – lightweight deterministic Node.js REST service for internal platform validation",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test test/**/*.test.js",
    "start": "node src/index.js"
  }
}
